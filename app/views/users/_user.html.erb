<li>
	<h4><%=link_to user.login, user %>
	<small>(
	<%=link_to "#{user.parselets.count} parselets", parselets_path(:user => user.key)%>
	)</small>
	</h4>
	<ol>
		<%=render Parselet.find(:all, :conditions => {:user_id => user.id})%>
	</ol>
</li>