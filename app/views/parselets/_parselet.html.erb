<li class="result">
  <%=thumb(parselet)%>
	<h4><%=status(parselet, :small) %><%=link_to h(parselet.name), parselet %>
	</h4>
	<h6 class="domain"><%=link_to truncate(parselet.example_url, :length => 40), parselet.example_url%></h6>
	<p><%=h parselet.description %>
</li>